# Memory-Leak-Demo-in-C-Azure-VM-Test-
A simple C program that repeatedly allocates memory to demonstrate how memory leaks occur, how they impact system RAM and virtual memory, and the performance effects observed when running the leak on a 16GB Azure VM.
